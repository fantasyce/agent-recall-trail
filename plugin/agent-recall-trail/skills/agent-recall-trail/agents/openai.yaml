interface:
  display_name: "Agent Recall Trail"
  short_description: "Recall private experience and reviewed shared knowledge."
  default_prompt: "Use $agent-recall-trail to recall relevant evidence and apply its shared value standard to worthwhile private memory during ordinary work. Check automatic-memory state; explicit user requests declare a sanitized request basis. Open ART's local page for human governance."
  brand_color: "#2F6B5F"
policy:
  allow_implicit_invocation: true
